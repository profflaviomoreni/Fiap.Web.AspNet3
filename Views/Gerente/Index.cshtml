@model IEnumerable<Fiap.Web.AspNet3.Models.GerenteModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sobrenome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sobrenome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GerenteId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GerenteId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GerenteId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
